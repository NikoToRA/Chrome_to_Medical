namespace WindowsNativeAssistant.Models
{
    /// <summary>
    /// Application state enumeration
    /// </summary>
    public enum AppState
    {
        Idle,
        PanelOpen,
        Generating,
        Pasting,
        Success,
        Failure
    }
}
