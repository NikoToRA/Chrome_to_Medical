<Window x:Class="WindowsNativeAssistant.UI.MainPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Windows Native Assistant"
        Width="400"
        Height="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False"
        KeyDown="Window_KeyDown">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="AI診療記録生成"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- Template Selection -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="テンプレート選択:"
                       FontWeight="SemiBold"
                       Margin="0,0,0,5"/>
            <ComboBox x:Name="TemplateComboBox"
                      DisplayMemberPath="Name"
                      SelectedIndex="0"
                      Height="30"/>
        </StackPanel>

        <!-- User Input -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="入力内容（箇条書き/メモ）:"
                       FontWeight="SemiBold"
                       Margin="0,0,0,5"/>
            <TextBox x:Name="UserInputTextBox"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Height="200"
                     Padding="5"/>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <Button x:Name="GenerateButton"
                    Content="生成して貼り付け"
                    Height="40"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Background="#4CAF50"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="GenerateButton_Click"/>
        </StackPanel>

        <!-- Status/Progress -->
        <StackPanel Grid.Row="4">
            <ProgressBar x:Name="ProgressBar"
                        Height="4"
                        IsIndeterminate="False"
                        Visibility="Collapsed"
                        Margin="0,0,0,10"/>
            <TextBlock x:Name="StatusTextBlock"
                      Text=""
                      FontSize="12"
                      TextAlignment="Center"
                      Foreground="#666"/>
        </StackPanel>
    </Grid>
</Window>
