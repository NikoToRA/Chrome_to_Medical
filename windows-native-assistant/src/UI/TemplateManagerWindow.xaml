<Window x:Class="WindowsNativeAssistant.UI.TemplateManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="定型文管理"
        Width="450"
        Height="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- カテゴリ管理セクション -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="カテゴリ管理" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

            <!-- 新規カテゴリ追加 -->
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="NewCategoryInput" Padding="8,6" BorderBrush="#ddd"
                         VerticalContentAlignment="Center" PlaceholderText="新しいカテゴリ名"/>
                <Button Grid.Column="1" Content="追加" Click="AddCategory_Click"
                        Margin="8,0,0,0" Padding="12,6"
                        Background="#10b981" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            </Grid>

            <!-- カテゴリリスト -->
            <ItemsControl x:Name="CategoryList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </StackPanel>

        <!-- 定型文管理セクション -->
        <StackPanel Grid.Row="1">
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="定型文管理" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
                <ComboBox x:Name="CategorySelector" Grid.Column="1" Margin="10,0,0,0"
                          DisplayMemberPath="Name" SelectedValuePath="Id"
                          SelectionChanged="CategorySelector_SelectionChanged"/>
            </Grid>

            <!-- 定型文リスト -->
            <Border BorderBrush="#ddd" BorderThickness="1" CornerRadius="4" Padding="5" Height="150">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="TemplateList"/>
                </ScrollViewer>
            </Border>
        </StackPanel>

        <!-- 定型文追加 -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="NewTemplateInput" Padding="8,6" BorderBrush="#ddd"
                     AcceptsReturn="False" VerticalContentAlignment="Center"/>
            <Button Grid.Column="1" Content="定型文追加" Click="AddTemplate_Click"
                    Margin="8,0,0,0" Padding="12,6"
                    Background="#2563eb" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </Grid>

        <!-- 閉じるボタン -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="閉じる" Click="Close_Click" Padding="20,8"
                    Background="#f0f0f0" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
