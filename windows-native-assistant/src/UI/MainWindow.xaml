<Window x:Class="WindowsNativeAssistant.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Karte AI+ Assistant"
        Width="350"
        Height="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        Topmost="True"
        ShowInTaskbar="True"
        KeyDown="Window_KeyDown"
        Closing="Window_Closing">

    <Window.Resources>
        <!-- カテゴリボタンスタイル -->
        <Style x:Key="CategoryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#f0f0f0"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#e0e0e0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 選択中のカテゴリボタンスタイル -->
        <Style x:Key="CategoryButtonSelectedStyle" TargetType="Button" BasedOn="{StaticResource CategoryButtonStyle}">
            <Setter Property="Background" Value="#2563eb"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <!-- 定型文ボタンスタイル -->
        <Style x:Key="TemplateButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="BorderBrush" Value="#ddd"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#f5f5f5"/>
                    <Setter Property="BorderBrush" Value="#2563eb"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="#fafafa">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <Border Grid.Row="0" Background="White" BorderBrush="#eee" BorderThickness="0,0,0,1" Padding="15,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Karte AI+" FontSize="18" FontWeight="Bold" Foreground="#333"/>
                <Button Grid.Column="1" Content="管理" Click="ManageTemplates_Click"
                        Background="Transparent" BorderThickness="0" Foreground="#666" Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- カテゴリタブ -->
        <Border Grid.Row="1" Background="White" Padding="10,8">
            <StackPanel x:Name="CategoryPanel" Orientation="Horizontal"/>
        </Border>

        <!-- 定型文リスト -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="10,5">
            <StackPanel x:Name="TemplateListPanel"/>
        </ScrollViewer>

        <!-- 新規追加エリア -->
        <Border Grid.Row="3" Background="White" BorderBrush="#eee" BorderThickness="0,1,0,0" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="NewTemplateInput"
                         Padding="8,6"
                         BorderBrush="#ddd"
                         BorderThickness="1"
                         VerticalContentAlignment="Center"
                         KeyDown="NewTemplateInput_KeyDown"/>
                <Button Grid.Column="1" Content="追加" Click="AddTemplate_Click"
                        Margin="8,0,0,0" Padding="12,6"
                        Background="#2563eb" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- ステータスバー -->
        <Border Grid.Row="4" Background="#f0f0f0" Padding="10,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusText" Text="定型文をクリックで貼り付け" Foreground="#666" FontSize="11"/>
                <CheckBox x:Name="DirectPasteToggle" Grid.Column="1" Content="直接貼付"
                          IsChecked="True" Checked="DirectPasteToggle_Changed" Unchecked="DirectPasteToggle_Changed"
                          VerticalContentAlignment="Center" FontSize="11" Foreground="#666"/>
            </Grid>
        </Border>
    </Grid>
</Window>
